---
server:
  port: ${SERVER_PORT:8080}

spring:
  datasource:
    url: "jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/\
      ${DB_NAME:deployment_service}"
    username: ${DB_USERNAME:deployment_service}
    password: ${DB_PASSWORD:change-me}

  jpa:
    hibernate:
      ddl-auto: ${HIBERNATE_DDL_AUTO:update}
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://auth.kleff.io/application/o/kleff/
          jwk-set-uri: https://auth.kleff.io/application/o/kleff/jwks/

  sql:
    init:
      platform: postgres
      mode: ${SQL_INIT_MODE:always}
