spring.application.name=demo

# Default audit service URL for local/dev
# Override in production (e.g. via environment variable or deployment config)
audit.service.url=http://project-management-service:8080/api/v1/projects/audit/internal
