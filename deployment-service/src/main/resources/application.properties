spring.application.name=demo

# Default audit service URL for Kubernetes (uses full DNS name)
# Override in production (e.g. via environment variable or deployment config)
audit.service.url=http://localhost:8080/api/v1/projects/audit/internal