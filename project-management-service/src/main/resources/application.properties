spring.application.name=project-management-service

# Hibernate DDL Auto - create-drop will wipe data on each restart (useful for development)
spring.jpa.hibernate.ddl-auto=create-drop

# Authorization System Configuration
# Shadow mode is controlled via database feature_flags table
# Enable debug logging for authorization decisions
logging.level.com.kleff.projectmanagementservice.authorization=DEBUG
